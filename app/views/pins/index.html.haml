#pins.masonry-container.transitions-enabled.infinite-scroll
  - @pins.each do |pin|
    .container.col-md-4
      .box.panel.panel-default
        = link_to (image_tag pin.image.url(:medium)), pin
        .panel-body
          %h2= link_to pin.title, pin
          %p.user
          Submitted by:
          = pin.user.email
